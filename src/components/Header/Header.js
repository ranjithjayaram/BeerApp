import React from 'react';
import styles from './Header.scss';

const Header = props => (
	<div className="header">
		<label className="header-text">Beer App</label>
	</div>
);

export default Header;
